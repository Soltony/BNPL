// NOTE: This folder is legacy/unreferenced by Next.js App Router.
// Keep it as a thin re-export so `tsc --noEmit` stays green.
export { POST, DELETE } from '@/app/api/settings/data-provisioning-uploads/route';
import { getSession } from '@/lib/session';

